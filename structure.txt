.
├── .env.example                 # Шаблон для переменных окружения (пароли, ключи)
├── .gitignore                   # Игнорируем .env, __pycache__, данные Postgres и т.д.
├── README.md                    # Главный файл с описанием проекта и инструкцией по запуску
│
├── services/                     # Папка для сервисов инфраструктуры (то, что мы поднимаем в первую очередь)
│   ├── mlflow/
│   │   ├── docker-compose.yml   # Поднимает БД и сам MLflow (сборка из Dockerfile)
│   │   ├── Dockerfile           # Сборка образа MLflow
│   │   └── .env                 # (будет скопирован из .env.example)
│   ├── airflow/
│   │   ├── docker-compose.yml   # Запуск Airflow (webserver, scheduler, БД)
│   │   ├── dags/                # Папка для DAG-файлов (будет примонтирована)
│   │   └── .env
│   ├── lakefs/
│   │   ├── docker-compose.yml   # Запуск LakeFS + Postgres + MinIO
│   │   └── .env
│   ├── jupyterhub/
│   │   ├── docker-compose.yml   # Запуск JupyterHub
│   │   ├── Dockerfile           # Сборка образа с JupyterHub и прокси
│   │   ├── jupyterhub_config.py # Конфиг JupyterHub
│   │   └── .env
│   └── monitoring/               # Папка для мониторинга (Этап 6)
│       ├── docker-compose.yml   # Запуск Prometheus и Grafana
│       ├── prometheus/
│       │   └── prometheus.yml   # Конфиг для сбора метрик
│       └── .env
│
├── ml_service/                   # Папка с кодом самого ML-сервиса (Этап 5)
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py              # Точка входа FastAPI
│   │   ├── api/
│   │   │   └── v1/
│   │   │       └── endpoints.py # Эндпоинты (/predict)
│   │   ├── core/
│   │   │   └── config.py        # Настройки сервиса (из переменных окружения)
│   │   ├── models/
│   │   │   └── model.py         # Логика загрузки и инференса модели
│   │   ├── schemas/              # Pydantic модели (для валидации запросов/ответов)
│   │   │   └── predict.py
│   │   └── utils/
│   │       ├── logger.py        # JSON-логирование
│   │       ├── metrics.py       # Эндпоинт /metrics и счетчики Prometheus
│   │       └── db_logger.py     # Логирование в БД
│   ├── requirements.txt         # Зависимости для ML-сервиса
│   ├── Dockerfile               # Сборка образа ML-сервиса
│   └── docker-compose.yml       # (Опционально) для локального запуска сервиса
│
├── kubernetes/                   # Папка для манифестов (Этап 7)
│   ├── ml-service/
│   │   ├── deployment.yaml
│   │   ├── service.yaml
│   │   └── ingress.yaml
│   └── (другие сервисы, если нужно)
│
└── helm/                         # Папка для Helm-чарта (Этап 8)
    └── ml-service-chart/         # Название чарта
        ├── Chart.yaml
        ├── values.yaml           # Значения по умолчанию (версии образов, ресурсы)
        └── templates/
            ├── deployment.yaml
            ├── service.yaml
            ├── ingress.yaml
            └── _helpers.tpl